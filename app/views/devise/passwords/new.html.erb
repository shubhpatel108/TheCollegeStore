<div class="container" style="margin-bottom:100px;">
	<div class="row">
	<div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1 panel">
		<div class="panel-header">
		    <h2><i class="fa"></i>Resend Password</h2>
		</div>

		<div class="panel-body">
			<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { :class => "smart-form", method: :post }) do |f| %>
			<div class="row">
			<div class="col-md-8">
				<%= f.email_field :email, autofocus: true, :placeholder => 'Email', :class => "form-control", :id => 'email' %>
			</div>
			<div class="col-md-4">
	      	<%= f.submit "Reset Password", :class => 'form-control btn btn-primary' %>
	      	</div>
	      	</div>
	      	<br />
	      	<!--
	      	<center>

				<%- if controller_name != 'sessions' %>
	  				<%= link_to "Sign in", new_session_path(resource_name) %>
				<% end -%>
				<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
				  <%= link_to "Sign up", new_registration_path(resource_name) %>
				<% end -%>
			</center>
			-->
		  <% end %>
		 </div>
	</div>
	<div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1 panel">
		<div class="panel-header">
		    <h2><i class="fa"></i>Reset Instructions</h2>
		</div>
		<div class="panel-body">
			<p>
			Forgot your password? Life happens, dont worry, we've got you covered. Put your login ID/email in the above box, and we'll send you an email with a link to reset your password. Be sure to click on that link within 24 hours!
			</p>
		 </div>
	</div>
	</div>
</div>