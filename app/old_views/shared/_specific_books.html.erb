<div class="container_4">
    <div class="innertitle">
        <p>Books for category: <%=category.name%></p>
    </div>
    <div class="no-touch">
      <ul class="grid cs-style-3">
        <% books.each do |book| %>
          <%= render :partial => "shared/book_tile", :locals => {:book => book}%>
        <% end %>
      </ul>
    </div>
</div>
